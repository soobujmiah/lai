# core:scheduler

Pure-JVM backend routing. A backend is eligible only when compile and runtime-probe evidence exist. Vulkan/QNN additionally require physical-device validation. Memory budget, thermal state and low-battery policy can reject acceleration. Measured performance is used only when a real value exists; no synthetic benchmark is accepted.
