# CI build trigger

This file exists to trigger the Android CI pipeline after verifying the workflow configuration.

The Android workflow builds the debug and release APKs on pushes to `gpu/opencl-qualification-probe`.

No runtime behavior is changed by this file.
