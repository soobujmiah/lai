# plugins:api

Versioned contract for future built-in or signed LAI capabilities. All manifests are local-only and schema-defined. Dynamic third-party code loading is intentionally disabled until signature verification, compatibility, revocation, storage and Android authority isolation are designed and device-tested.
